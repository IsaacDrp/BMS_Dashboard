version: '3.8'

services:
  # Nombre de nuestro servicio
  monitor-dashboard:
    build: .
    
    # 2. Nombre del contenedor (opcional, pero útil)
    container_name: monitor-dashboard-prod
    
    # 3. Mapeo de puertos 8081:80
    ports:
      - "8081:80"
    restart: unless-stopped

networks:
  default:
    driver: bridge